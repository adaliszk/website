import { defineConfig } from "@adaliszk/qwik-storybook"

const { stories, core, framework, addons, docs } = defineConfig();
const config = { stories, core, framework, addons, docs };
export default config;
