{
    "version": "0.1.0",
    "name": "@adaliszk.io/design",
    "description": "Collection of utility components and helpers on top of Daisy UI and Tailwind.",
    "license": "BSD 2-Clause",
    "types": "dist/bundle.d.ts",
    "main": "dist/bundle.js",
    "type": "module",
    "scripts": {
        "lint": "biome lint ./src",
        "build": "tsup src/bundle.ts --clean --minify --dts --format esm --out-dir dist"
    },
    "dependencies": {
        "@adaliszk/qwik": "^1.5.4",
        "@tailwindcss/typography": "^0.5.13",
        "daisyui": "^4.10.5",
        "tailwind-merge": "^2.3.0",
        "tailwindcss": "^3.4.3"
    },
    "exports": {
        ".": {
            "types": "./dist/bundle.d.ts",
            "import": "./dist/bundle.js"
        }
    }
}
