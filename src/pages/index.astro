---
// import { getCollection } from "astro:content";
//
// export const Articles = (await getCollection("articles")).pick(6);
// export const articles$ = serializeForQwik(Articles);
//
// export const WelcomeMessageData = (await getCollection("messages")).choose();
// export const WelcomeMessage = (await WelcomeMessageData.render()).Content;

import ProjectsPage from "./projects.astro";
---

<ProjectsPage/>
